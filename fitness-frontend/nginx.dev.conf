# —— 1. 裸 IP（HTTP） ——
# server {
#     listen 80;
#     server_name 8.208.16.103;

#     # 你希望裸 IP 直接展示内容？取消下面的 redirect 就行
#     return 301 https://8.208.16.103.sslip.io$request_uri;
# }
# 1. 先处理本地开发：Host: localhost
server {
    listen      80  default_server;    # 明确指定默认
    server_name _;

    root        /usr/share/nginx/html;
    index       index.html;

    # SPA 路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 如果有本地 API 代理也在这里写
    # 2) API → Spring-Boot container
    location /api/ {
         # Remove the /api prefix and then forward
        rewrite ^/api(/.*)$ $1 break;
        proxy_pass         http://backend:8080;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
# 1) 所有 http 请求都重定向到 https
# server {
#   listen 80;
#   server_name 8.208.16.103.sslip.io;    # 指定你的 sslip.io 子域名
# #   server_name  localhost;
#   return 301 https://$host$request_uri;
# }
# server {
#     # listen 80;
#     # server_name _;

#     # 线上环境
#     listen 443 ssl http2;
#     server_name 8.208.16.103.sslip.io;
#     # Let’s Encrypt 证书（由 ./certs 挂载）
#     ssl_certificate     /etc/letsencrypt/live/8.208.16.103.sslip.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/8.208.16.103.sslip.io/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;

#     # 本地测试
#     # listen       443 ssl;
#     # server_name  localhost;
#     # ssl_certificate     /path/to/certs/localhost.crt;
#     # ssl_certificate_key /path/to/certs/localhost.key;

#     # open gzip
#     gzip              on;
#     gzip_disable      "msie6";
#     gzip_vary         on;
#     gzip_proxied      any;
#     gzip_comp_level   6;
#     gzip_buffers      16 8k;
#     gzip_min_length   256;
#     gzip_http_version 1.1;
#     gzip_types
#         text/plain
#         text/css
#         application/json
#         application/javascript
#         text/xml
#         application/xml
#         application/xml+rss
#         text/javascript;

#     # 1) Static assets (React build)
#     root   /usr/share/nginx/html;
#     index  index.html;

#     # HTML no cache
#     location = /index.html {
#         add_header Cache-Control "no-cache, must-revalidate" always;
#     }

#     location / {
#         try_files $uri /index.html;    # SPA-routing
#     }

#     # Only perform long caching on the original file (without pre-compression)
#     location ~* \.(?:js|css|map|woff2?|png|jpe?g|gif|svg)$ {
#         # Return the gzip file. If not found, it will result in a 404 error
#         # try_files $uri =404;
#         try_files $uri $uri.br $uri.gz =404;
#         # Force the browser to cache for one year without revalidation. "immutable" means the content will not change
#         add_header Cache-Control "public, max-age=31536000, immutable" always;
#         # Expires
#         expires 1y;
#         # Close the log and reduce I/O
#         access_log off;
#     }

#     # 2) API → Spring-Boot container
#     location /api/ {
#          # Remove the /api prefix and then forward
#         rewrite ^/api(/.*)$ $1 break;
#         proxy_pass         http://backend:8080;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }


# # 1) 所有 http 请求都重定向到 https
# server {
#   listen 80;
#   server_name 8.208.16.103.sslip.io;    # 指定你的 sslip.io 子域名
#   return 301 https://$host$request_uri;
# }
# server {
#     # listen 80;
#     # server_name _;
#     listen 443 ssl http2;
#     server_name 8.208.16.103.sslip.io;

#     ssl_certificate     /etc/ssl/certs/fullchain.pem;
#     ssl_certificate_key /etc/ssl/private/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;

#     # open gzip
#     gzip              on;
#     gzip_disable      "msie6";
#     gzip_vary         on;
#     gzip_proxied      any;
#     gzip_comp_level   6;
#     gzip_buffers      16 8k;
#     gzip_min_length   256;
#     gzip_http_version 1.1;
#     gzip_types
#         text/plain
#         text/css
#         application/json
#         application/javascript
#         text/xml
#         application/xml
#         application/xml+rss
#         text/javascript;

#     # 1) Static assets (React build)
#     root   /usr/share/nginx/html;
#     index  index.html;

#     # HTML no cache
#     location = /index.html {
#         add_header Cache-Control "no-cache, must-revalidate" always;
#     }

#     location / {
#         try_files $uri /index.html;    # SPA-routing
#     }

#     # Only perform long caching on the original file (without pre-compression)
#     location ~* \.(?:js|css|map|woff2?|png|jpe?g|gif|svg)$ {
#         # Return the gzip file. If not found, it will result in a 404 error
#         # try_files $uri =404;
#         try_files $uri $uri.br $uri.gz =404;
#         # Force the browser to cache for one year without revalidation. "immutable" means the content will not change
#         add_header Cache-Control "public, max-age=31536000, immutable" always;
#         # Expires
#         expires 1y;
#         # Close the log and reduce I/O
#         access_log off;
#     }

#     # 2) API → Spring-Boot container
#     location /api/ {
#          # Remove the /api prefix and then forward
#         rewrite ^/api(/.*)$ $1 break;
#         proxy_pass         http://backend:8080;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }